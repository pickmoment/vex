# VEX — Visual EXplorer

> "파일을 보는 새로운 방식 — 터미널 안의 뷰어"

VEX는 Rust로 구현된 TUI(Terminal User Interface) 파일 매니저입니다.  
파일 **보기(View)** 경험에 집중하고, 하단 힌트 바와 마우스 지원으로 직관적인 조작을 제공합니다.

---

## 목차

1. [설치](#설치)
2. [UI 레이아웃](#ui-레이아웃)
3. [단축키](#단축키)
4. [기능](#기능)
5. [파일 관리](#파일-관리)
6. [Git 관리 모드](#git-관리-모드)
7. [설정](#설정)
8. [기술 스택](#기술-스택)
9. [개발 로드맵](#개발-로드맵)
10. [기여 가이드](#기여-가이드)

---

## 설치

**사전 요구사항:** Rust 1.75+ (`rustup` 권장)

```bash
git clone https://github.com/example/vex
cd vex

# 릴리즈 빌드
cargo build --release
./target/release/vex

# 또는 PATH에 설치
cargo install --path .
```

---

## UI 레이아웃

### 파일 탐색 모드 (기본)

```
┌─ VEX | /home/user/projects ──────────────────────── [?] 도움말 ─┐
├─ 즐겨찾기 ──┬─ 파일 목록 ─────────────────┬─ 미리보기 ────────────┤
│  Home      │ ▶  src/              │  # VEX                    │
│  projects  │    Cargo.toml  M     │                           │
│            │    README.md         │  VEX는 Rust로...           │
│            │    .gitignore        │                           │
├────────────┴─────────────────────┴───────────────────────────┤
│ ↑↓:이동  Enter:열기메뉴  →:뷰어/진입  m:파일관리  /:검색  q:종료  │
└───────────────────────────────────────────────────────────────┘
```

파일 이름 옆의 `M`(노란색) · `A`(초록색) · `D`(빨간색) · `?`(회색) 배지는 Git 상태를 나타냅니다.

### 전체화면 뷰어 모드 (Space)

```
┌─ README.md ──────────────────────────── [w:줄바꿈  e:편집기] ─┐
│  # VEX — Visual EXplorer                                    │
│                                                              │
│  VEX는 Rust로 구현된 TUI 파일 매니저입니다.                    │
│                                                              │
│  ## 기능                                                     │
│  - 마크다운 렌더링                                            │
│  - 신택스 하이라이팅                                          │
│                                                              │
│ ──────────────────────────────────── 12%  q:닫기  /:검색     │
└──────────────────────────────────────────────────────────────┘
```

---

## 단축키

### 파일 탐색

| 키 | 동작 |
|----|------|
| `↑` / `k` | 위로 이동 |
| `↓` / `j` | 아래로 이동 |
| `PageUp` / `PageDown` | 10칸 이동 |
| `←` / `h` | 상위 폴더 |
| `→` / `l` | 폴더 진입 또는 뷰어 열기 |
| `Enter` | 열기 프로그램 선택 메뉴 |
| `Space` | 전체화면 뷰어 |
| `/` | 파일명 검색 |
| `m` | 파일 관리 메뉴 (복사·이동·삭제·이름변경) |
| `n` | 새 폴더 생성 |
| `r` | 파일 목록 새로고침 |
| `y` | 현재 경로 클립보드 저장 |
| `b` | 현재 폴더 즐겨찾기 추가/제거 |
| `Tab` | 즐겨찾기 패널 포커스 전환 |
| `g` | Git 관리 모드 |
| `?` | 단축키 도움말 |
| `Ctrl+P` | 명령어 팔레트 |
| `Ctrl+,` | 설정 화면 |
| `q` | 종료 |

### 전체화면 뷰어

| 키 | 동작 |
|----|------|
| `q` / `Esc` | 파일 목록으로 돌아가기 |
| `↑`/`↓` / `j`/`k` | 줄 스크롤 |
| `PageUp` / `PageDown` / `Space` | 빠른 스크롤 |
| `←` / `→` | 수평 스크롤 |
| `gg` / `G` | 파일 처음 / 끝 |
| `:숫자` | 지정 줄로 이동 |
| `/` | 내용 검색 |
| `n` / `N` | 다음 / 이전 매칭 |
| `w` | 줄바꿈 토글 |
| `e` | `$EDITOR`로 열기 |

### 즐겨찾기 패널

| 키 | 동작 |
|----|------|
| `↑` / `↓` | 항목 이동 |
| `Enter` / `→` | 해당 폴더로 이동 |
| `d` | 즐겨찾기 삭제 |
| `Tab` / `Esc` | 파일 목록으로 돌아가기 |

---

## 기능

### 마크다운 렌더링

`pulldown-cmark` 기반으로 파싱하여 TUI 위젯으로 렌더링합니다.

지원 요소: H1~H6 제목, **굵기**, *기울임*, `인라인 코드`, 코드 블록, 테이블, 인용구, 체크리스트, 수평선

### 신택스 하이라이팅

`syntect` 기반으로 170개 이상의 언어를 지원합니다.

Rust, Python, JavaScript/TypeScript, Go, C/C++, Java, Shell, TOML, YAML, JSON, HTML, CSS 등

### 이미지 미리보기

터미널 환경을 자동 감지하여 최적의 프로토콜로 렌더링합니다.

| 프로토콜 | 지원 터미널 |
|----------|------------|
| Kitty | kitty |
| iTerm2 | iTerm2 (macOS) |
| Sixel | WezTerm, xterm 등 |
| Braille | 모든 터미널 (폴백) |

지원 포맷: PNG, JPEG, GIF, WEBP, BMP

### CSV 테이블 뷰어

CSV/TSV 파일을 컬럼 정렬된 인터랙티브 테이블로 표시합니다.

### 파일 검색

`/` 키로 현재 디렉토리에서 파일명 실시간 필터링, 매칭 부분 강조 표시.

---

## 파일 관리

파일 탐색 중 `m` 키로 파일 관리 메뉴를 엽니다.

```
┌─ 파일 관리: README.md ──────────────────────────────────────────┐
│                                                                  │
│ ▶  복사       대상 경로로 복사              [c]                   │
│    이동       대상 경로로 이동              [v]                   │
│    이름 변경  현재 디렉토리에서 이름 변경    [r]                   │
│    삭제       파일/디렉토리 삭제             [d]                   │
│    새 폴더    현재 디렉토리에 새 폴더 생성   [n]                   │
│                                                                  │
│  ↑↓:이동  Enter:선택  Esc/q:취소                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 메뉴 단축키

| 키 | 동작 |
|----|------|
| `↑` / `↓` | 항목 이동 |
| `Enter` | 선택한 작업 실행 |
| `c` | 복사로 바로 이동 |
| `v` | 이동으로 바로 이동 |
| `r` | 이름 변경으로 바로 이동 |
| `d` | 삭제로 바로 이동 |
| `n` | 새 폴더 생성으로 바로 이동 |
| `Esc` / `q` | 메뉴 닫기 |

### 복사 / 이동

대상 경로 입력 창이 열립니다. 현재 디렉토리가 기본값으로 채워집니다.
`Tab` 키로 경로 클립보드에서 미리 저장한 경로를 선택할 수 있습니다.

```
┌─ 복사: README.md ──────────────────────────────────────────────┐
│                                                                 │
│  원본: README.md                                                │
│                                                                 │
│  대상 경로: /home/user/backup/█                                 │
│                                                                 │
│  Enter:확인  Tab:경로목록  Esc:메뉴로                            │
└─────────────────────────────────────────────────────────────────┘
```

대상 경로가 이미 존재하면 덮어쓰기 여부를 확인합니다 (`y` 확인 / `n` 취소).

### 이름 변경

현재 파일명이 입력창에 미리 채워집니다.

```
┌─ 이름 변경: README.md ─────────────────────────────────────────┐
│                                                                 │
│  원본: README.md                                                │
│                                                                 │
│  새 이름: README_backup.md█                                     │
│                                                                 │
│  Enter:확인  Esc:메뉴로                                          │
└─────────────────────────────────────────────────────────────────┘
```

### 삭제

`y` 키로 삭제를 확인합니다. 디렉토리는 하위 내용 포함 전체 삭제됩니다.

```
┌─ 삭제 확인 ─────────────────────────────────────────────────────┐
│                                                                  │
│  ⚠  정말 삭제하시겠습니까?                                        │
│                                                                  │
│  README.md                                                       │
│                                                                  │
│  y:삭제  n/Esc:취소                                              │
└──────────────────────────────────────────────────────────────────┘
```

작업 완료 후 파일 목록과 Git 상태가 자동으로 갱신됩니다. 성공·실패 결과는 하단 상태 바에 토스트 메시지로 표시되며 3~5초 후 자동으로 사라집니다.

---

## Git 관리 모드

파일 탐색 중 `g` 키로 진입합니다. Git 저장소가 아닌 경우 안내 메시지를 표시합니다.

### 파일 상태 관리

```
┌─ Git: main  스테이징 2  변경 3 ────────────────────────────────┐
├─ 스테이징 (2) ─────────┬─ diff: src/app.rs ────────────────────┤
│ ▶  M  src/app.rs      │ diff --git a/src/app.rs b/src/app.rs │
│    A  src/git/mod.rs  │ @@ -10,5 +10,20 @@                   │
├─ 워킹 트리 (3) ─────── │  pub struct App {                    │
│    M  Cargo.toml      │ +    pub git_diff_wrap: bool,         │
│    M  README.md       │ -    old_field: usize,                │
│    ?? src/new_file.rs │                                       │
├───────────────────────┴───────────────────────────────────────┤
│ a:스테이지  u:언스테이지  c:커밋  f:전체화면  L:로그  q:닫기        │
└────────────────────────────────────────────────────────────────┘
```

파일을 선택하면 오른쪽 diff 패널에 내용이 즉시 표시됩니다.

| 키 | 동작 |
|----|------|
| `↑` / `↓` | 파일 이동 (diff 자동 갱신) |
| `Tab` | 스테이징 ↔ 워킹 트리 전환 |
| `a` | 선택 파일 스테이지 |
| `u` | 선택 파일 언스테이지 |
| `c` | 커밋 메시지 입력 후 커밋 |
| `r` | 상태 새로고침 |
| `L` | 커밋 로그 패널 열기/닫기 |
| `q` | 파일 탐색으로 돌아가기 |

### 커밋 로그 탐색

`L` 키로 3패널 로그 뷰로 전환됩니다.

```
┌─ 커밋 로그 ────────────┬─ 변경 파일 ──────┬─ diff ──────────────┐
│ ▶ abc1234 Fix bug     │ ▶  M src/app.rs  │ diff --git a/...   │
│   def5678 Add feature │    A src/new.rs  │ @@ -5,3 +5,7 @@    │
│   ghi9012 Initial     │    D old_file.rs │ -old line          │
│                       │                  │ +new line          │
├───────────────────────┴──────────────────┴────────────────────┤
│ ↑↓:커밋이동  →/Enter:파일목록  ↑↓:파일이동  f:전체화면  q:닫기     │
└────────────────────────────────────────────────────────────────┘
```

| 단계 | 키 | 동작 |
|------|----|------|
| 로그 열기 | `L` | 커밋 로그 패널 표시 |
| 커밋 목록 | `→` / `l` | 커밋 목록 포커스 진입 |
| | `↑` / `↓` | 커밋 이동 (변경 파일 자동 로드) |
| | `→` / `Enter` | 변경 파일 목록 포커스 진입 |
| 변경 파일 | `↑` / `↓` | 파일 이동 (diff 자동 로드) |
| | `←` / `Esc` | 커밋 목록으로 돌아가기 |

### diff 뷰어 조작

파일 diff 및 커밋 diff 모두 동일한 조작키를 사용합니다.

| 키 | 동작 |
|----|------|
| `PageUp` / `PageDown` | 수직 스크롤 |
| `[` / `]` | 수평 스크롤 (±4칸) |
| `w` | 줄바꿈 ON/OFF |
| `f` | diff 전체화면 |
| `↑`/`↓`, `←`/`→`, `PageUp`/`PageDown`/`Space` | 전체화면 내 스크롤 |
| `Esc` / `f` | 전체화면 닫기 |

---

## 설정

설정 파일 위치:

| OS | 경로 |
|----|------|
| macOS | `~/Library/Application Support/vex/config.toml` |
| Linux | `~/.config/vex/config.toml` |
| Windows | `%APPDATA%\vex\config.toml` |

```toml
[general]
show_hidden = false
sort_by = "Name"
sort_descending = false

[ui]
show_icons = true   # Nerd Font 필요
show_hint_bar = true

[preview]
wrap = false
syntax_theme = "Solarized (dark)"
max_file_size = 10485760  # 10MB

# 외부 프로그램 등록 (Enter → 열기 메뉴에서 선택 가능)
[[openers]]
name = "nvim"
command = "nvim"
args = []
terminal = true

[[openers]]
name = "VS Code"
command = "code"
args = []
terminal = false

# 즐겨찾기
bookmarks = [
  "/home/user/projects",
  "/home/user/Documents",
]
```

---

## 기술 스택

| 레이어 | 크레이트 |
|--------|---------|
| TUI 프레임워크 | `ratatui` 0.28 |
| 비동기 런타임 | `tokio` 1.x |
| 터미널 이벤트 | `crossterm` 0.28 |
| 마크다운 파싱 | `pulldown-cmark` 0.11 |
| 신택스 하이라이팅 | `syntect` 5.x |
| 이미지 렌더링 | `ratatui-image` + `image` |
| 파일시스템 감시 | `notify` 6.x |
| CSV 파싱 | `csv` 1.x |
| 설정 관리 | `serde` + `toml` + `config` |
| 퍼지 검색 | `nucleo` |
| 에러 처리 | `anyhow` + `thiserror` |

---

## 개발 로드맵

### v0.1 — Foundation ✅ 완료

- [x] 3-패널 파일 탐색 (즐겨찾기 · 파일 목록 · 미리보기)
- [x] 텍스트 / 코드 미리보기 + 신택스 하이라이팅
- [x] 마크다운 렌더링 미리보기
- [x] 하단 힌트 바 + 마우스 스크롤 지원
- [x] 파일명 검색 (실시간 필터 + 매칭 강조)
- [x] 즐겨찾기 (추가/삭제/이동)
- [x] 열기 프로그램 선택 메뉴
- [x] 외부 편집기 연동 (`$EDITOR`)

### v0.2 — Viewer ✅ 완료

- [x] 전체화면 뷰어 모드
- [x] 이미지 미리보기 (Kitty / iTerm2 / Sixel / Braille 폴백)
- [x] CSV 테이블 뷰어
- [x] 뷰어 내 검색 (`/`, `n`, `N`)
- [x] 줄 이동 (`:숫자`), gg/G 점프
- [x] 줄바꿈 토글 (`w`)

### v0.3 — Git & File Management 🚧 진행 중

- [x] **Git 관리 모드** (`g`)
  - [x] 파일 상태 표시 (staged / unstaged / untracked)
  - [x] 파일 목록 Git 배지 (M · A · D · ?)
  - [x] Stage / Unstage / Commit
  - [x] diff 뷰어 (수직·수평 스크롤, 줄바꿈, 전체화면)
  - [x] 파일 선택 시 diff 자동 표시
  - [x] 커밋 로그 3패널 탐색 (커밋 → 변경 파일 → diff)
- [x] **파일 관리** (`m`, `n`)
  - [x] 복사 (대상 경로 지정, 덮어쓰기 확인)
  - [x] 이동 (대상 경로 지정, 덮어쓰기 확인)
  - [x] 이름 변경
  - [x] 삭제 (확인 후 실행, 디렉토리 항목수 경고)
  - [x] 새 폴더 생성 (`n` 단축키로 바로 진입)
  - [x] 경로 클립보드 (`y` 저장, 복사·이동 시 `Tab`으로 선택)
  - [x] 작업 결과 토스트 피드백 (성공/실패 3~5초 표시)
- [ ] PDF 미리보기
- [ ] ZIP / tar 내부 탐색
- [ ] TUI 설정 화면 (현재 TOML 직접 편집)
- [ ] 탭 기능

### v1.0 — Stable

- [ ] 명령어 팔레트 기능 완성
- [ ] 원격 파일시스템 (SFTP)
- [ ] 플러그인 API

---

## 기여 가이드

```bash
# 의존성 확인
cargo check

# 개발 빌드 및 실행
cargo run

# 테스트
cargo test

# 포맷 / 린트
cargo fmt
cargo clippy
```

PR은 `cargo fmt`와 `cargo clippy` 통과를 기준으로 리뷰합니다.

---

## 라이선스

MIT License — [LICENSE](LICENSE) 파일 참조.
